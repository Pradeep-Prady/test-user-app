import { VariableDirective } from './variable.directive';

describe('VariableDirective', () => {
  it('should create an instance', () => {
    const directive = new VariableDirective();
    expect(directive).toBeTruthy();
  });
});
